export interface FlightDto { 
    // id: number,
    // plane: Object,
    origin: string,
    destination: string, 
    departureTime: Date,
    arrivalTime: Date,
    delayInMins: number,
    price: number,
    seatsCount: number
}